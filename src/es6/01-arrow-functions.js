function square(num) {   
    return num * num;       // funcion 
}

const square = (num) => {
    return num * num;       // funcion en arrow function
}

const square = num => num * num;  // funcion con return implicito